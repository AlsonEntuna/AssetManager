<Window
    x:Class="AssetManager.View.AssetManagerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helix="http://helix-toolkit.org/wpf/SharpDX"
    xmlns:local="clr-namespace:AssetManager"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:AssetManager.ViewModel"
    Title="Asset Manager"
    Width="1500"
    Height="850"
    Background="{StaticResource Main.Background}"
    ResizeMode="CanResizeWithGrip"
    mc:Ignorable="d">
    <Window.DataContext>
        <viewmodels:AssetManagerViewModel />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Command="{Binding LoadFileCommand}" Header="Load File" />
            </MenuItem>
            <MenuItem Header="Options">
                <MenuItem Command="{Binding PerforceSetupCommand}" Header="Perforce Setup" />
                <Separator />
                <MenuItem Command="{Binding SetupOfflineModeCommand}" Header="Setup Offline View" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid
                Grid.Row="1"
                Grid.Column="0"
                Panel.ZIndex="1"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                <Border Background="{StaticResource Main.Background}" Opacity="0.5">
                    <Border.BorderBrush>
                        <SolidColorBrush Opacity="0.5" Color="#000000" />
                    </Border.BorderBrush>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="18"
                            Style="{StaticResource TextBlock.Main}"
                            Text="Loading..." />
                        <ProgressBar
                            Width="200"
                            Height="30"
                            IsIndeterminate="True" />
                    </StackPanel>
                </Border>
            </Grid>
            <Expander
                Grid.Row="0"
                Grid.Column="0"
                Foreground="{StaticResource Text.Main}"
                Header="P4"
                IsEnabled="{Binding IsConnected, UpdateSourceTrigger=PropertyChanged}">
                <GroupBox Header="P4" Style="{StaticResource GroupBox.Main}">
                    <StackPanel>
                        <GroupBox Header="Workspace">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Text="Workspace" />
                                    <ComboBox
                                        x:Name="Combo_Clients"
                                        Grid.Column="1"
                                        ItemsSource="{Binding Workspaces, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        SelectedIndex="0"
                                        SelectedItem="{Binding Client, UpdateSourceTrigger=PropertyChanged}"
                                        SelectionChanged="Combo_Clients_SelectionChanged" />
                                </Grid>
                                <GroupBox Header="Depot Path">
                                    <TextBox Text="{Binding DepothPah, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                                </GroupBox>
                                <Button
                                    Margin="5"
                                    Command="{Binding SyncCommand}"
                                    Content="Sync"
                                    IsEnabled="{Binding IsConnected, UpdateSourceTrigger=PropertyChanged}"
                                    Style="{StaticResource ButtonStyle.Main}" />
                            </StackPanel>
                        </GroupBox>
                        <Button
                            Margin="5"
                            Command="{Binding OpenRootFolderCommand}"
                            Content="Open Root Folder"
                            Style="{StaticResource ButtonStyle.Main}" />
                    </StackPanel>
                </GroupBox>
            </Expander>
            <Grid Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Style="{StaticResource TextBlock.Main}"
                            Text="Type: " />
                        <ComboBox
                            x:Name="Combo_ObjType"
                            Grid.Column="1"
                            VerticalContentAlignment="Center"
                            ItemContainerStyle="{StaticResource ComboBoxItem.Main}"
                            SelectedItem="{Binding SelectedObjType, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            SelectionChanged="Combo_ObjType_SelectionChanged" />
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            Style="{StaticResource TextBlock.Main}"
                            Text="Search:" />
                        <TextBox
                            x:Name="Txt_Search"
                            Grid.Column="1"
                            VerticalContentAlignment="Center"
                            TextChanged="Txt_Search_TextChanged" />
                    </Grid>
                    <DataGrid
                        x:Name="DataGrid_Objects"
                        Grid.Row="2"
                        Margin="0,10,0,0"
                        AutoGenerateColumns="False"
                        Background="{StaticResource Main.Background}"
                        CanUserAddRows="False"
                        EnableRowVirtualization="True"
                        HorizontalScrollBarVisibility="Auto"
                        ItemsSource="{Binding ObjectDisplay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedItem="{Binding SelectedObj, UpdateSourceTrigger=PropertyChanged}"
                        VerticalScrollBarVisibility="Auto">
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Command="{Binding PreviewCommand}" Header="Preview">
                                    <MenuItem.Icon>
                                        <Image Source="{StaticResource Preview}" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Command="{Binding BrowseToPathCommand}" Header="Browse to Path">
                                    <MenuItem.Icon>
                                        <Image Source="{StaticResource BrowseFolder}" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="P4">
                                    <MenuItem Header="Checkout">
                                        <MenuItem.Icon>
                                            <Image Source="{StaticResource P4.Checkout}" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Mark for Delete" />
                                </MenuItem>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Name}" Header="Name" />
                            <DataGridTextColumn Binding="{Binding Type}" Header="Type" />
                            <DataGridTextColumn Binding="{Binding Path}" Header="Path" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
            <GridSplitter
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="1"
                Width="3"
                Margin="2,0,2,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Background="White" />

            <Grid
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0">
                        <Expander Foreground="{StaticResource Text.Main}" Header="Renderer Settings">
                            <StackPanel>
                                <CheckBox
                                    Content="Render Env Map"
                                    Foreground="White"
                                    IsChecked="{Binding RenderEnvironmentMap, UpdateSourceTrigger=PropertyChanged}" />
                                <Button
                                    Command="{Binding ResetCameraCommand}"
                                    Content="Reset Camera"
                                    Style="{StaticResource ButtonStyle.Main}" />
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                    <helix:Viewport3DX
                        Grid.Row="1"
                        Margin="0,10,0,0"
                        BackgroundColor="#595959"
                        Camera="{Binding Camera}"
                        EffectsManager="{Binding EffectsManager}"
                        EnableSwapChainRendering="True"
                        FXAALevel="Low"
                        FixedRotationPoint="{Binding ModelCentroid}"
                        FixedRotationPointEnabled="True">
                        <helix:Viewport3DX.InputBindings>
                            <KeyBinding Key="B" Command="helix:ViewportCommands.BackView" />
                            <KeyBinding Key="F" Command="helix:ViewportCommands.FrontView" />
                            <KeyBinding Key="U" Command="helix:ViewportCommands.TopView" />
                            <KeyBinding Key="D" Command="helix:ViewportCommands.BottomView" />
                            <KeyBinding Key="L" Command="helix:ViewportCommands.LeftView" />
                            <KeyBinding Key="R" Command="helix:ViewportCommands.RightView" />
                            <KeyBinding Command="helix:ViewportCommands.ZoomExtents" Gesture="Control+E" />
                            <MouseBinding Command="helix:ViewportCommands.Rotate" Gesture="LeftClick" />
                            <MouseBinding Command="helix:ViewportCommands.Zoom" Gesture="MiddleClick" />
                            <MouseBinding Command="helix:ViewportCommands.Pan" Gesture="RightClick" />
                        </helix:Viewport3DX.InputBindings>
                        <helix:AmbientLight3D Color="#363636" />
                        <helix:DirectionalLight3D Direction="{Binding Camera.LookDirection}" Color="#D6D6D6" />
                        <helix:EnvironmentMap3D IsRendering="{Binding RenderEnvironmentMap}" Texture="{Binding EnvironmentMap}" />
                        <helix:Element3DPresenter Content="{Binding GroupModel}" />
                        <helix:PostEffectMeshBorderHighlight EffectName="highlight" />
                    </helix:Viewport3DX>
                </Grid>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
